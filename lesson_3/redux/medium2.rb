CORE CONCEPT
- a method can have more than one return value. use multiple assignment to retrieve the return values
-


def not_so_tricky_method(a_string_param, an_array_param)
  a_string_param += "rutabaga"
  an_array_param += ["rutabaga"]

  return a_string_param, an_array_param
end

my_string = "pumpkins"
my_array = ["pumpkins"]
my_string, my_array = not_so_tricky_method(my_string, my_array)

puts "My string looks like this now: #{my_string}"
puts "My array looks like this now: #{my_array}"


def color_valid(color)
  %w(blue green).include?(color)
end


def tricky_method(a_string_param, an_array_param)
  a_string_param += "rutabaga"
  an_array_param << "rutabaga"
end

my_string = "pumpkins"
my_array = ["pumpkins"]
tricky_method(my_string, my_array)

puts "My string looks like this now: #{my_string}"
puts "My array looks like this now: #{my_array}"

# "My string looks like this now: pumpkins"
# "My array looks like this now: [pumpkins, rubataga]"
# one method mutates the caller (push) while the other does not (+=)
